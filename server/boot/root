module.exports = function(app) {
  var router = app.loopback.Router();
  router.get('../client/index.html', function(req, res) {
    res.send('');
  });
  router.get('../client/myalbums.html', function(req, res) {
    res.send('');
  });
  app.use(router);
}
